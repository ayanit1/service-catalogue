type Query {
  services: [Service!]!
  service(name: String!): Service
}

type Service {
  name: String!
  owner: String
  repo: String
  envVars: [envVar]
}

type envVar {
  name: String
  value: String
}
