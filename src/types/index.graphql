type Query {
  services: [Service!]!
  service(name: String!): Service
}

type Service {
  name: String!
  owner: String
  repo: String
  pods: Int
  isOnPaas: Boolean
  envVars: [envVar]
  urls: [String]
}

type envVar {
  name: String
  value: String
}
